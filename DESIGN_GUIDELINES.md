# Refuge Pro 设计准则

这份文件是所有后续 UI 修改的验收基线。新增页面、组件和视觉修补都必须继续遵守这些规则。

## 视觉语言

- 生产页面统一使用最新 iOS Human Interface Guidelines 的层级、留白、动态反馈和可读性原则。
- Android 端使用 Compose 原生实现；液态玻璃材料参考 [compose-hig](https://github.com/ienground/compose-hig) 和 [AndroidLiquidGlass](https://github.com/Kyant0/AndroidLiquidGlass)，不得用静态渐变或不透明占位替代交互材质。
- 深色和浅色主题都必须保持清晰的文字、图标、边界和焦点对比度。背景只使用明确的纯黑或纯白基底与受控的玻璃层。
- 图标使用项目图标库或 Lucide/Material 对应图标，按钮的图标尺寸、触控区域和对齐方式保持统一。

## 导航与弹窗

- 根导航顺序固定为：机库、商店、终端、我的。页面切换使用方向一致的轻量动画，切换过程中共享的头像和顶部结构不跳动。
- BottomSheet 必须在当前页面上原地入场，底层内容在动画期间持续可见；关闭时使用同一条退场动画。
- 弹窗底部操作按钮悬浮在内容上方，不绘制遮挡内容的独立底部遮罩或黑色矩形。
- 弹窗返回按钮使用圆形图标按钮，不使用“返回上一步”文字按钮；按钮圆角与弹窗顶角采用同一连续圆角几何并保持共圆心。
- 弹窗高度按内容和安全区计算，紧凑选择器不得默认占满屏幕；内容过长时只让内容区域滚动。

## 数据与性能

- 所有远程数据采用本地缓存优先、后台刷新；打开页面先展示最近一次有效缓存，网络成功后原位替换，不因刷新失败清空页面。
- 页面切换不重复创建共享仓库、玻璃背景或导航栏；预加载应放在应用启动后的后台协程中。
- 加载指示只用于用户主动下拉刷新或明确的首次空数据场景，普通页面切换不得显示全屏加载图标。
- 与 RSI、Spectrum、Erkul 的数据交互必须保留来源和失败回退；账号状态等写入操作先更新界面，再在后台同步并可重试。

## 验收流程

- 每次版本更新必须先运行 `testDebugUnitTest` 和 `assembleDebug`。
- 在模拟器上检查浅色、深色、头像状态弹窗、BottomSheet 入场/退场、页面切换和下拉刷新；检查 Logcat 无崩溃和明显卡顿。
- 更新 `CHANGELOG.md`、`version.properties`，再生成同版本 APK 并作为 GitHub Release 附件发布。

## 逐页导航检查（硬性）

- 每次交付前必须逐页检查机库、商店、终端、我的以及所有详情页的顶部和底部导航。
- 导航栏高度、头像/标题/右侧操作的水平基线保持一致；按钮触控区至少 44dp。
- 导航栏必须保留真实液态玻璃的模糊、折射、边缘高光和按下反馈，浅色与深色分别核对。
- 弹窗出现时导航栏仍留在原坐标，由弹窗内容覆盖其绘制层；不得先移除导航栏再打开弹窗。
- 点击文字、图标和滑块都使用同一套可打断页面切换动画；重复点击当前页面时，非顶部回到顶部，顶部执行刷新。
- 顶部渐变只作用于内容安全区，不得压暗头像和顶部操作；浅色从白色渐隐，深色从黑色渐隐。
